---
name: memforge-memory
description: Use when you need local project memory search, context compilation, or human-confirmed memory saving through MemForge.
---

Use the MemForge MCP server tools when available:

- `search_memory` for targeted retrieval.
- `compile_context` or `get_project_context` before coding.
- `save_memory` only after human confirmation.

Do not auto-scan repositories or persist unreviewed candidate memories.
